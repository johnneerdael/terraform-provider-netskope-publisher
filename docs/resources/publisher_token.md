---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "npa_publisher_token Resource - npa"
subcategory: ""
description: |-
  A one-shot registration token for an NPA publisher. Issued by POSTing to /api/v2/infrastructure/publishers/{id}/registration_token. The token is sensitive and is consumed by npa_publisher_wizard -token <token> on the publisher VM during first-boot cloud-init. To re-issue, taint this resource (force-replace).
---

# npa_publisher_token (Resource)

A one-shot registration token for an NPA publisher. Issued by POSTing to `/api/v2/infrastructure/publishers/{id}/registration_token`. The token is sensitive and is consumed by `npa_publisher_wizard -token <token>` on the publisher VM during first-boot cloud-init. To re-issue, taint this resource (force-replace).

## Example Usage

```terraform
resource "npa_publisher_token" "primary" {
  publisher_id = 42 # numeric publisher_id from the Netskope tenant
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `publisher_id` (Number) Numeric publisher ID this token belongs to. Changing it replaces the resource (issues a fresh token).

### Read-Only

- `id` (Number) Equal to `publisher_id`.
- `token` (String, Sensitive) The registration token. Sensitive; consumed once by the publisher wizard.
